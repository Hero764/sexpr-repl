#include <stdio.h>

#include "sexpr.h"
#include "tokenizer.h"

char *evaluate(char *

int main (int argc, char **argv)
{
	char line[1024];

	while (fgets(line, 1024, stdin) != NULL) {
	}

	return 0;
}
